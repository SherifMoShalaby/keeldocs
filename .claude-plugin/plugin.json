{
  "name": "keeldocs",
  "description": "Deterministic doc-drift detection: init (repo map + doc lie-detector), check (drift/verify/coverage), sync (reviewable patches), new (recipes).",
  "version": "0.5.0",
  "author": { "name": "keeldocs" }
}
