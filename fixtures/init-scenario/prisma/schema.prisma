datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Status {
  ACTIVE
  ARCHIVED
}

model Item {
  id     Int    @id @default(autoincrement())
  name   String
  status Status @default(ACTIVE)
}
