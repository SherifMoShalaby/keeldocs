# Deliverables 9 & 10 — Risk Register and Validation Experiments

## 1. Risk register (top risks, each with a concrete de-risking experiment and pass threshold)

| # | Risk | Why it kills | Mitigation in design | De-risk experiment → pass threshold |
|---|---|---|---|---|
| R1 | **Noise death** (ignored-Dependabot) | Trust, then usage, evaporates; 11.3% of projects deprecated Dependabot largely over volume (verified) | Fact-hash drift scope; noise SLO as tested spec; accept-rate self-throttle; journal-backed rejection memory; single amended rollup PR | E2 replay: drift FP <10% (v0.1), <5% (v0.2); E9 field trial: accept-rate ≥30% over 4 weeks |
| R2 | **Anchor churn / false drift** | Every phantom stale flag spends credibility the wow earned | Drift over extracted facts, not bytes; `h1:` versioned hashes; re-baseline on provider-set change; `unresolvable` ≠ drift | E2 history replay: ≥95% anchor survival over 6 months, zero human input |
| R3 | **Wrong auto-rebind** (silent misbinding) | A doc quietly describing the wrong symbol is worse than an orphan — it's fabrication by machinery | Two-signal single-candidate rule; S3 never auto; adjudications journaled | E3 rename corpus: false auto-rebind <0.5% (hard go/no-go), file-move rebind ≥99% |
| R4 | **Declarative tier under-recalls flagship frameworks** | A wrong/missing endpoint in the flagship artifact is a lie at first contact | Express = first-party code provider; declarative tier scoped to decorator frameworks; precision > recall policy; gaps as facts | E1 corpus (~30 repos/framework): decorator frameworks ≥90% recall ≥98% precision; Express code provider ≥95%/≥98% |
| R5 | **First-run wow insufficient → no adoption** | Drift value needs weeks; if init doesn't convert, nothing else runs | Lie-detector with receipts + committed map artifacts, zero LLM, <5 min | E4 user test (10 brownfield devs): ≥60% commit the starter docs; ≥8/10 verify a lie-detector finding as true; p50 init <5 min |
| R6 | **Nondeterminism regression** | Poisons drift at the root; unfalsifiable "works on my machine" bugs | Enumerated lattice, JCS, banned inputs, no floats; golden tests | E5: byte-identical facts+resolution, 3 OSes × 2 runs, every CI run, forever |
| R7 | **Agent-API churn** | Adapter rot consumes the maintainer; brief demands 2-year correctness | Bet on CLI + Agent Skills open standard; adapters ≤300 LOC, path-maps only; drop-don't-block release policy | E7 smoke matrix: auto-invoke + headless drift detection green on Claude Code/Codex/Cursor weekly; one deliberate breaking-change drill ≤1 week fix |
| R8 | **Secret leakage into generated docs** | One leaked DSN/API key = breach + rotation + reputational kill for a *docs* tool | Structural value-blindness; redaction barrier (gitleaks ruleset + entropy) on every write; history excerpts scanned pre-context; stderr scrubbing | E6 adversarial corpus (seeded secrets in env/migrations/git history/driver errors): 0 escapes to artifacts; FP rate on hash-heavy content <2% |
| R9 | **Supply-chain compromise of the tool itself** | Shai-Hulud-class worm through a doc tool with repo read access in CI | Provenance attestations; no install scripts; near-zero deps; pinned invocation docs; T2 machinery before any marketplace | CI-enforced budgets: runtime deps ≤5, install scripts = 0, provenance attestation verified on every release; one external security review before v0.2 with all critical/high findings fixed |
| R10 | **Scale wall on monorepos** | >15s warm checks → tool abandoned in the repos that need it most | Incremental extraction cached at the provider-subprocess boundary, keyed on the resolved input set by CONTENT hash (`src/cache.js`, 2026-08-01) — content rather than git index blob hashes, because the index needs stat-based dirty detection whose failure mode is a silently stale answer. Still unbuilt: provider output sharding (D2), per-file work inside a provider (D4), `--affected` (D3) | E8 1M-LOC synthetic + 2 real monorepos: warm check p50 ≤5s / p95 ≤15s; cold ≤10 min; RAM ≤2GB. **MEASURED 2026-08-01 → FAIL, then D1 built and re-measured → PASSES at 10k and 100k** (`experiments/e8-scale/RESULTS.md`). Original failure: no cache existed at all, so a one-file edit cost a full re-extraction (5.6s / 9.7s / 32.6s). After D1: warm check 1.40s @10k and 2.23s @100k, RAM 891-897 MB, cache overhead 33 ms. Still failing: 1M exits 2 on the ADR-002 output cap (D2), and a one-file edit at 100k is 6.32s because 12 providers declare the changed file and each re-parses all 1,400 of its declared files (D4). Budgets never moved. The 2-real-monorepo half of this gate is still owed |
| R11 | **Prompt injection via docsmith artifacts** | Tool-taught trust in attacker-influenceable files amplifies an agent-ecosystem weakness | Schema-strict anchors/journal; CLI-mediated fact access with untrusted framing; mined text opt-in/quoted/capped; human-review sync; no auto-acting | E10 red-team: crafted anchors/facts/PR text in a hostile fixture repo; injection must not survive to an executed action in the three first-class agents; residual risk documented |
| R12 | **Maintainer burnout / bus factor 1** | The most common OSS death | Tier-A cap ~10; monthly train; fixture harness lowers review cost; co-maintainer hard gate at v1.0 | Tracked: maintainer hours/provider-PR <4h; ≥2 maintainers before v1.0 (gate, not hope) |
| R13 | **Mermaid limits break the flagship ERD** | An unreadable or truncated diagram at first contact | Budget-driven chunking in `erdChunks` (40k chars / 400 edges per fenced diagram, under Mermaid's 50k/500); schema-first then greedy name-order packing; ≤25 entities per diagram; edges filtered to the picture that draws them; omissions stated in the artifact; canonical emission | E11: 500-table fixture renders layered, readable, within maxTextSize 50k / maxEdges 500 (verified defaults). **MEASURED 2026-08-01 → FAILED, REDESIGNED, NOW PASSES** (`experiments/e11-erd-scale/RESULTS.md`): the flat diagram crossed maxTextSize between 100 and 250 tables and maxEdges at ~250 — R13 was real and nearer than estimated. Chunking added; every size to 1,000 tables now renders with every table drawn. Gated permanently by `tests/erd.test.js` (asserts Mermaid's real ceilings, not the internal budget) and a 260-table end-to-end harness check |
| R14 | **Name collision** | npm `docsmith` is AGPL-taken; PyPI taken; first re-pick "undrift" turned out to be an existing company's live CLI (Homebrew tap found during the lock step) | **Resolved 2026-07-30: shipped name = "keeldocs"** — verified free on npm, PyPI, and GitHub | Remaining pass condition: crates.io + same-category trademark sweep clean for "keeldocs" before v0.1 announce; publish the placeholder packages promptly (registries move — the undrift near-miss proves it) |

## 2. Validation experiments, priority-ordered (cheapest falsifiers of core assumptions first)

Ordering principle: each experiment falsifies a *load-bearing* assumption; run before or alongside the build, not after.

1. **E1 — Extraction recall/precision corpus** (~1 week, no product code needed beyond prototypes). Label endpoints/tables/env-vars in ~30 OSS repos per framework (NestJS, Express, Prisma). Falsifies: "the declarative tier covers the easy 80%" and "Express needs the code tier." Pass: table in R4. *If decorator-framework recall <90%, the two-tier provider thesis itself is wrong — redesign before building the engine.*
2. **E2 — History-replay harness** (~1–2 weeks; becomes permanent CI infrastructure). Replay 6–12 months of 5–10 real repos; compute anchor survival, drift FP/FN, re-anchoring events. Falsifies: "fact-hash drift is low-noise" (R1/R2), the product's core bet. Pass: survival ≥95%, FP <10% v0.1.
3. **E3 — Re-anchoring accuracy corpus** (piggybacks on E2 + synthetic refactors). Falsifies: "evidence-gated rebinding is safe." Pass: false auto-rebind <0.5%; thresholds per ADR-007.
4. **E4 — Lie-detector wow test** (~3 days with a prototype over E1's corpus + 10 users). Falsifies: "deterministic verification wows on first run" (R5 — the adoption bet). Pass: ≥60% commit starter docs; ≥8/10 confirm a finding.
5. **E5 — Determinism golden tests** (1 day to stand up; permanent). Falsifies: "byte-identical output is achievable across OSes." Pass: empty diff, 3 OSes × 2 runs.
6. **E6 — Redaction adversarial corpus** (~3 days). Falsifies: "the write barrier catches realistic leaks." Pass: 0 escapes, FP <2%.
7. **E7 — Agent adapter smoke matrix** (2 days; weekly thereafter). Falsifies: "Agent Skills standard actually yields cross-agent auto-invoke reliability" (the distribution bet; the standard's *uniform behavior* is the assumption, its existence is verified). Pass: green on 3 first-class agents.
8. **E8 — Scale benchmark** (after engine skeleton). Pass: budgets in R10. **Run 2026-08-01 → FAIL; D1 built the same day; re-run → passes at 10k and 100k.** The original failure was not a slow cache but a missing one, which is the kind of thing only a measurement finds — the risk register had listed the mitigation as if it existed. With incremental extraction the 100k warm check is 2.23s against a 5s budget. Two named failures remain: 1M LOC still fails closed on the provider output cap (D2), and a one-file edit at 100k costs 6.32s because a provider's unit of work is its whole declared input set rather than the file that changed (D4). Comfortable ceiling today is ~100k LOC / 200 packages at a ~2s warm check. `experiments/e8-scale/RESULTS.md`.
9. **E9 — Noise SLO field trial** (v0.1 beta cohort, 4 weeks). Pass: accept-rate ≥30%, no user disables the nudge in week 1.
10. **E10 — Injection red-team** (before v0.2). Pass: R11.
11. **E11 — ERD scale rendering** (2 days). Pass: R13. **Run 2026-08-01 → failed, redesigned, passes.** The flat ERD exceeded Mermaid's `maxTextSize` between 100 and 250 tables — a size band real Supabase and Rails schemas sit in — and would have rendered *nothing* on the flagship artifact. The threshold was not moved; `src/render.js` gained budget-driven chunking. `experiments/e11-erd-scale/RESULTS.md`.
12. **E12 — Study verification** (half a day): obtain full text of "To Copilot and Beyond" (2026) to confirm/kill the "~46% / Continuous Documentation Synchronizer top-rank" claim before it appears in any positioning material.

Experiments E1–E4 together cost roughly three calendar weeks and can falsify the four assumptions the whole design stands on: extractability, low-noise drift, safe rebinding, and the zero-LLM wow. If any fails its threshold, the correct move is redesign, not adjustment of the threshold.
